# 🚀 الدليل الشامل لتشغيل وتحديث المشروع على سيرفر Beget VPS

تم إعداد هذا الدليل باللغة العربية ليوضح بدقة وبساطة كيفية **تشغيل المشروع بالكامل** على السيرفر، وكيفية **إجراء أي تحديثات مستقبلية** من GitHub وتشغيلها بنجاح 100% دون أي أخطاء.

---

## 📌 1. بيانات السيرفر والمشروع الأساسية

| العنصر | القيمة / البيان |
| :--- | :--- |
| **اسم الدومين** | `классный-фокус.рф` |
| **الدومين بصيغة البونيكود (Punycode)** | `xn----7sb1acdcpkxafxk9g.xn--p1ai` |
| **مسار المشروع على السيرفر** | `/var/www/video-courses` |
| **رابط لوحة تحكم الأدمن** | `https://классный-фокус.рф/admm` |
| **بيانات الدخول للأدمن** | تُضبط في ملف `.env` عبر `ADMIN_EMAIL` و `ADMIN_PASSWORD` |
| **بيانات قاعدة البيانات (PostgreSQL)** | تُضبط في ملف `.env` عبر `DATABASE_URL` (محمية ومغلقة عن الوصول الخارجي) |


---

## ⚡ 2. طريقة التحديث السريع من GitHub (في خطوتين فقط)

كلما قمت بعمل تعديلات جديدة على كود المشروع على جهازك الكمبيوتر وتريد تطبيقها على السيرفر:

### الخطوة الأولى: من جهازك الكمبيوتر
افتح الطرفية (Terminal أو Git Bash) في مجلد المشروع وشغّل:
```bash
git add .
git commit -m "update: أضف وصف التعديلات هنا"
git push origin main
```

### الخطوة الثانية: على السيرفر
اتصل بالسيرفر عبر SSH:
```bash
ssh root@5.35.87.221
```
ثم شغّل هذا الأمر الواحد فقط:
```bash
cd /var/www/video-courses && bash deployment/deploy.sh
```

> **ماذا يفعل سكريبت `deploy.sh` تلقائياً؟**
> 1. يسحب أحدث كود من GitHub (`git pull`).
> 2. يتأكد من تشغيل حاويات Docker (قاعدة البيانات والتخزين MinIO والـ Redis).
> 3. ينشئ مجلدات تخزين الفيديوهات وصلاحياتها في MinIO.
> 4. يثبت أي حزم ومكتبات جديدة (`pnpm install`).
> 5. يطبق أي تحديثات على جداول قاعدة البيانات (`db run push`).
> 6. يبني الواجهة الأمامية والخلفية (`run build`).
> 7. يحدّث إعدادات Nginx ويعيد تشغيل السيرفر عبر PM2 دون انقطاع.

---

## 🛠️ 3. طريقة تشغيل المشروع بالكامل لأول مرة على السيرفر

إذا أردت تشغيل وتجهيز كل جزء في المشروع يدوياً خطوة بخطوة:

### أ. الدخول إلى مجلد المشروع
```bash
cd /var/www/video-courses
```

### ب. تشغيل حاويات Docker (قاعدة البيانات + التخزين MinIO + Redis)
```bash
docker compose up -d || docker-compose up -d
```
*للتأكد من أنها تعمل وصحية:*
```bash
docker ps
```

### ج. إعداد باكتات التخزين في MinIO للفيديوهات والصور
```bash
docker exec video-courses-minio sh -c "
  mc alias set local http://localhost:9000 minioadmin minioadmin123 &&
  mc mb local/videos-prod --ignore-existing &&
  mc mb local/videos-dev --ignore-existing &&
  mc mb local/video-courses --ignore-existing &&
  mc anonymous set download local/videos-prod &&
  mc anonymous set download local/videos-dev &&
  mc anonymous set download local/video-courses
"
```

### د. إنشاء مجلد المعالجة المؤقت للفيديوهات
```bash
sudo mkdir -p /var/tmp/video-processing
sudo chmod 777 /var/tmp/video-processing
```

### هـ. تطبيق ملف المتغيرات البيئية للإنتاج (`.env`)
```bash
cp deployment/.env.production.example .env
```

### و. تثبيت المكتبات وبناء المشروع
```bash
pnpm install
pnpm --filter @workspace/db run push
pnpm --filter @workspace/api-server run build
pnpm --filter @workspace/video-courses run build
```

### ز. إنشاء مستخدم الأدمن في قاعدة البيانات
```bash
node --env-file=.env ./node_modules/tsx/dist/cli.mjs ./scripts/src/seed-admin.ts
```

### ح. تشغيل خادم الـ API عبر PM2
```bash
pm2 restart ecosystem.config.cjs --update-env || pm2 start ecosystem.config.cjs
pm2 save
```

### ط. تطبيق إعدادات خادم الويب Nginx
```bash
sudo cp deployment/nginx.conf /etc/nginx/sites-available/video-courses.conf
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🗄️ 4. إدارة وتشغيل قاعدة البيانات (PostgreSQL)

* **تشغيل قاعدة البيانات فقط:**
  ```bash
  docker start video-courses-postgres
  ```

* **إعادة تشغيل قاعدة البيانات:**
  ```bash
  docker restart video-courses-postgres
  ```

* **الدخول المباشر إلى سطر أوامر قاعدة البيانات (PostgreSQL CLI):**
  ```bash
  docker exec -it video-courses-postgres psql -U video_user -d video_courses_db
  ```
  - لعرض جميع الجداول: `\dt`
  - لعرض المستخدمين: `SELECT id, email, role FROM users;`
  - للخروج: `\q`

* **تطبيق أي تعديل جديد على الجداول (Migrations):**
  ```bash
  pnpm --filter @workspace/db run push
  ```

---

## 🔍 5. أوامر المراقبة وفحص الأخطاء (Troubleshooting)

* **لمتابعة سجلات وأخطاء خادم الـ API مباشرة:**
  ```bash
  pm2 logs video-courses-api --lines 50
  ```

* **لمعرفة حالة الـ API واستهلاك الذاكرة:**
  ```bash
  pm2 status
  ```

* **لفحص حالة Nginx والتأكد من عدم وجود أخطاء:**
  ```bash
  sudo nginx -t
  sudo systemctl status nginx
  ```

* **لفحص استجابة الموقع والخادم:**
  ```bash
  curl -I https://xn----7sb1acdcpkxafxk9g.xn--p1ai/api/healthz
  ```
اوامر التشغيل بالخطوات :
1- git stash
git pull origin main
git stash pop

2- bash deployment/deploy.sh


3- pm2 restart all